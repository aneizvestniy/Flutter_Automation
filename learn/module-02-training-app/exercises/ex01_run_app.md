# Exercise 01 — Run the training app

## Goal
Be able to run the Flutter app locally (Android emulator recommended).

## Task
1) Install Flutter (or use FVM in this repo once configured).
2) Run:
   - `flutter doctor`
   - `flutter pub get`
   - `flutter run`
3) Confirm you can:
   - open login screen
   - login (test credentials)
   - navigate to CRUD list

## Acceptance criteria
- App runs on emulator/device.
- Learner can explain where to find:
  - screens/widgets
  - use cases
  - repositories
