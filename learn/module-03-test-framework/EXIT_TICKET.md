# Module 3 — Exit Ticket

## Deliverable
Write your first runnable Appium E2E test:
- Login flow (5–7 steps)

## Acceptance criteria
- Test starts Appium session
- Uses Flutter finders (`byValueKey`, `text`, `type`)
- No raw `sleep()` without justification
- Test asserts a stable outcome (next screen key or error key)
