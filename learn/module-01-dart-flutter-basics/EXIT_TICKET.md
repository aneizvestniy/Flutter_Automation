# Module 1 — Exit Ticket

## Deliverable
Submit a short note (PR description or training chat message) with:

1) **DevTools proof**
- Screenshot or description: where you found the Login button in Widget Inspector

2) **Key added**
- Add at least 1 `ValueKey` using the registry in `app/lib/core/testing/keys.dart`

3) **Finder usage**
- Provide the finder you would use in tests:
  - `find.byValueKey('screen.login.submit')` (or the constant)

## Acceptance criteria
- All 3 items provided
- Key name follows convention
